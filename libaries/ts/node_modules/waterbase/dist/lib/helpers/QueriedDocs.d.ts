import { QueriedDocs as QD } from '../models/QueriedDocs';
import { Documents } from '../models/Documents';
declare class QueriedDocs implements QD {
    size: number;
    docs: Documents[];
    empty: boolean;
    query: object;
    constructor(query: object, docs: Documents[]);
}
export = QueriedDocs;
