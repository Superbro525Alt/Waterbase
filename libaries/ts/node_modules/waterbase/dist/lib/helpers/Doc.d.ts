import Client from '../Client';
declare class Doc {
    data: object;
    collection: string;
    client: Client;
    constructor(client: Client, collection: string, data: object);
}
export = Doc;
